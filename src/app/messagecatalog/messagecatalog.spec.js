describe('messagecatalog section', function () {
    beforeEach(module('zoli.messagecatalog'));

    it('should have a dummy test', inject(function() {
        expect(true).toBeTruthy();
    }));
});